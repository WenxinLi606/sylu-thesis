# 本次核验记录

核验日期：2026-09-23。

## 实际执行

- macOS、TeX Live 2023、XeLaTeX、latexmk 4.79、BibTeX。
- 默认 `portablefonts` 双面示例完整编译，生成 24 页 PDF（含原排版规则要求的空白背页）。没有自备字体依赖。
- `schoolfonts` 使用本地原有宋体、黑体和两份汉仪字体，以及已安装的 Times New Roman，完整编译通过。
- `oneside` 与增加第 8 章组合测试通过，正文模式仍启用；第 8 章正文基线间距确认为 21 bp。
- 从导出的纯源码 ZIP 解压后，重新完整编译，未借用旧 `.aux`、`.bbl` 或字体文件。
- 检查中英文扉页、声明、摘要、目录、正文、公式、图表、算法、参考文献、附录、致谢和成果页的渲染。
- 最终日志无编译错误、缺字、未定义引用、重复标签或 Overfull/Underfull 警告。CTeX/Fandol 的脚本标记提示及主动覆盖字体族产生的提示不影响此次编译。
- 扫描模板源码和 PDF 文本，原作者及导师等姓名、原研究题目与主要专有术语未残留。上游模板与文献样式的作者署名按来源说明保留。

## 保留与调整

保留原确认版的中文扉页坐标、字号、学校名、声明文本、页面尺寸、页边距、页眉、页码与图表格式。字体配置独立到 `sylu-fonts.tex`。正文排版不再限定为前七章；附录页眉改为“附录 A”等形式。

默认便携模式的字形与原确认版不同，不将其宣称为与 Word 完全一致。需要相应字形时，应启用自备字体模式并核对具体字体版本和填写后的页面。

## 未执行的验证

没有登录 Overleaf 创建远程项目，也没有在 Windows 或 Linux 真机上编译。Overleaf 使用说明依据标准 XeLaTeX + BibTeX + latexmk 工作流；已通过无自备字体的干净源码包编译检查，但这不等于已在 Overleaf 实机验收。

本记录说明本地编译验证范围，GitHub 发布状态不属于编译验证。公开 ZIP 不含本地保留的自备字体或最终 PDF；主目录 `main.pdf` 仅用于本次审核。

## 本次更新

三线表改为在浮动体初始化后居中，修复初始化覆盖居中设置的问题；固定特需项目名称；预留教授和研究员两位导师、三位评阅人；移除符号说明页及其源文件；中英文题目与 PDF 元数据改为模板标题。更新后的默认字体版与自备字体版均重新编译并检查，公开包重新从干净目录编译。

三线表三条横线的中心横坐标均为 297.64 bp，与 A4 页面中心一致；两种字体模式均已核对。
